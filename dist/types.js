// Tipos del módulo de check-in. Espejan el schema `kalai` de las
// migraciones 035–038 del repo de Coach Data (que es donde vive el
// schema — ver decisión D5 del relevamiento de Fase 0).
export {};
